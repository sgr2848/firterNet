import tensorflow as tf 
import numpy as np
import pandas as pd
import tensorflow import keras
from tensorflow.keras import Sequential
from  tensorflow.keras.layers import Dense, Flatten, LSTM 

def model(feed_text: List):
    model = Sequential()
    model.add()
    model.add()
    model.add()
    model.compile()
    model.fit()
    m
def analyse(some_text: str,model) -> int:
    pass    
